<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.webapps.mapper.IRecommendMapper">
	<resultMap id="resultMap" type="com.webapps.common.entity.Recommend">
		<id column="ID" jdbcType="DECIMAL" property="id" />
		<result column="user_id" jdbcType="DECIMAL" property="user.id" />
		<result column="user_name" jdbcType="DECIMAL" property="user.name" />
		<result column="user_mobile" jdbcType="DECIMAL" property="user.mobile" />
		<result column="recruitment_Id" jdbcType="DECIMAL" property="recruitment.id" />
		<result column="recruitment_title" jdbcType="DECIMAL" property="recruitment.title" />
		<result column="company_id" jdbcType="DECIMAL" property="company.id" />
		<result column="company_name" jdbcType="VARCHAR" property="company.name" />
		<result column="name" jdbcType="VARCHAR" property="name" />
		<result column="mobile" jdbcType="VARCHAR" property="mobile" />
		<result column="state" jdbcType="DECIMAL" property="state" />
		<result column="age" jdbcType="DECIMAL" property="age" />
		<result column="gender" jdbcType="DECIMAL" property="gender" />
		<result column="data_state" jdbcType="DECIMAL" property="dataState" />
		<result column="create_time" jdbcType="VARCHAR" property="createTime" />
		<result column="UPDATE_TIME" jdbcType="VARCHAR" property="updateTime" />
	</resultMap>

	<sql id="select_columns">
		tr.ID,tr.user_id,tr.recruitment_Id,tr.name,tr.mobile,tr.age,tr.gender,tr.state, tr.data_state,tr.CREATE_TIME,tr.UPDATE_TIME
	</sql>

	<sql id="insert_columns">
		user_id,recruitment_Id,name,mobile,age,gender,state, data_state,CREATE_TIME
	</sql>
	
	<select id="getById" resultMap="resultMap">
		select <include refid="select_columns" /> from t_recommend where id = #{id}
	</select>
	
	<!-- 查询所有 -->
	<select id="queryAll" resultMap="resultMap">
		select <include refid="select_columns" /> from t_recommend where data_state = 1
	</select>
	
	<!-- 查询所有 -->
	<select id="queryAllByDataState" resultMap="resultMap">
		select <include refid="select_columns" /> from t_recommend
		<if test="dataState != null and dataState != ''">
			where data_State = dataState 
		</if>
	</select>
	
	<!-- 根据父级行政区号查询 -->
	<select id="queryByUserId" resultMap="resultMap">
		select <include refid="select_columns" /> from t_recommend where user_id = #{obj.id} and data_state = 1
	</select>
	
	<!-- 根据行政区等查询 -->
	<select id="queryByRecruitmentId" resultMap="resultMap">
		select <include refid="select_columns" /> from t_recommend where recruitment_id = #{obj.recruitment.id} and data_state = 1
	</select>
	
	<!-- 查询行政区总数 -->
	<select id="queryCount" resultType="java.lang.Integer">
		SELECT count(1) FROM t_recommend tr 
		left join t_user tu on tr.user_id = tu.id
		left join t_recruitment tre on tr.recruitment_id = tre.id
		left join t_company tc on tc.id = tre.company_id
		<trim prefix="WHERE" prefixOverrides="AND">
			tr.data_state = 1 
			<if test="obj.name !=null and obj.name != ''">
				and tr.name like concat('%',#{obj.name},'%')
			</if>
			<if test="obj.mobile != null and obj.mobile != ''">
				and tr.mobile = #{obj.mobile,jdbcType=VARCHAR}
			</if>
			<if test="obj.user != null and obj.user.name != null and obj.user.name !='' ">
				and tu.name = #{obj.user.name,jdbcType=VARCHAR}
			</if>
			<if test="obj.company !=null and obj.company.name != null and obj.company.name != ''">
				and tc.name like concat('%',#{obj.company.name},'%') 
			</if>
			<if test="obj.recruitment != null and obj.recruitment.title != null and obj.recruitment.title != ''">
				and tre.title like concat('%',#{obj.recruitment.title},'%')
			</if>
		</trim>
	</select>

	<!-- 分页查询行政区信息 -->
	<select id="queryPage" resultMap="resultMap">
		<include refid="ORACLE.paginationStart" />
		SELECT
		<include refid="select_columns" />,
		tu.name as user_name,
		tu.mobile as user_mobile,
		tc.id as company_id,
		tc.name as company_name,
		tre.title as recruitment_title
		FROM t_recommend tr 
		left join t_user tu on tr.user_id = tu.id
		left join t_recruitment tre on tr.recruitment_id = tre.id
		left join t_company tc on tc.id = tre.company_id
		<trim prefix="WHERE" prefixOverrides="AND">
			tr.data_state = 1 
			<if test="obj.name !=null and obj.name != ''">
				and tr.name like concat('%',#{obj.name},'%')
			</if>
			<if test="obj.mobile != null and obj.mobile != ''">
				and tr.mobile = #{obj.mobile,jdbcType=VARCHAR}
			</if>
			<if test="obj.user != null and obj.user.name != null and obj.user.name !='' ">
				and tu.name = #{obj.user.name,jdbcType=VARCHAR}
			</if>
			<if test="obj.company !=null and obj.company.name != null and obj.company.name != ''">
				and tc.name like concat('%',#{obj.company.name},'%') 
			</if>
			<if test="obj.recruitment != null and obj.recruitment.title != null and obj.recruitment.title != ''">
				and tre.title like concat('%',#{obj.recruitment.title},'%')
			</if>
		</trim>
		order by name
		<include refid="ORACLE.paginationEnd" />
	</select>
	
	<insert id="insert" useGeneratedKeys="true" keyProperty="obj.id" parameterType="com.webapps.common.entity.Recommend">
		insert into t_recommend 
		(
			<include refid="insert_columns" />
		)
		values
		(
			#{obj.user.id},
			#{obj.recruitment.id},
			#{obj.name},
			#{obj.mobile},
			#{obj.age},
			#{obj.gender},
			#{obj.state},
			#{obj.dataState},
			#{obj.createTime}
		)
	</insert>
	
	<update id="updateById">
		update t_recommend 
		set 
			recruitment_id = #{obj.recruitment.id},
			name = #{obj.name},
			mobile = #{obj.mobile},
			state = #{obj.state},
			data_state = #{obj.dataState},
			age = #{obj.age},
			gender = #{obj.gender},
			update_time = now()
		where id = #{id}
	</update>
</mapper>