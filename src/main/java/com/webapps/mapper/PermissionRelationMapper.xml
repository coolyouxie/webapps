<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.webapps.mapper.IPermissionRelationMapper">
    <resultMap id="resultMap" type="com.webapps.common.entity.PermissionRelation">
        <id column="ID" jdbcType="DECIMAL" property="id"/>
        <result column="permission_id" jdbcType="VARCHAR" property="name"/>
        <result column="parent_permission_id" jdbcType="VARCHAR" property="code"/>
        <result column="create_time" jdbcType="VARCHAR" property="createTime"/>
        <result column="update_time" jdbcType="VARCHAR" property="updateTime"/>
        <result column="data_state" jdbcType="DECIMAL" property="dataState"/>
    </resultMap>

    <sql id="select_columns">
		ID,permission_id,parent_permission_id,create_time,update_time,data_state,create_time,update_time
	</sql>

    <sql id="insert_columns">
		permission_id,parent_permission_id,data_state,create_time
	</sql>

    <select id="getById" resultMap="resultMap">
        select
        <include refid="select_columns"/>
        from t_permission_relation where id = #{id}
    </select>

    <!-- 查询所有 -->
    <select id="queryAll" resultMap="resultMap">
        select
        <include refid="select_columns"/>
        from t_permission_relation
    </select>
    
    <select id="queryByPermissionId" resultMap="resultMap">
        select
        <include refid="select_columns"/>
        from t_permission_relation where permission_id = #{permissionId}
    </select>
    
    <select id="queryByParentId" resultMap="resultMap">
        select
        <include refid="select_columns"/>
        from t_permission_relation where parent_permission_id = #{parentId}
    </select>
    
    <select id="queryByConditions" resultMap="resultMap">
        select
        <include refid="select_columns"/>
        from t_permission_relation 
        <trim prefix="WHERE" prefixOverrides="AND">
            <if test="obj.permissionId !=null and obj.permissionId != ''">
                AND permission_id = #{obj.permissionId}
            </if>
            <if test="obj.parentPermissionId != null and obj.parentPermissionId != '' ">
                and parent_permission_id = #{obj.parentPermissionId}
            </if>
            <if test="obj.parentCode != null and obj.parentCode != ''">
                and parentCode =#{obj.paretCode}
            </if>
            <if test="obj.dataState != null and obj.dataState != '' or obj.dataState==0">
                and data_state = #{obj.dataState}
            </if>
        </trim>
    </select>

    <update id="batchDeleteInLogic">
        <foreach collection="list" item="item" index="index" open="" close="" separator=";">
            update t_permission_relation 
            <set>
                data_state = 0,
                update_time = #{item.updateTime}
            </set>
            where id = #{item.id}
        </foreach>
    </update>

    <insert id="insert" useGeneratedKeys="true" keyProperty="obj.id">
        insert into t_permission_relation 
        (<include refid="insert_columns"/>)
        values
        (
        #{obj.permissionId},
        #{obj.parentPermissionId},
        1,
        #{obj.createTime}
        )
    </insert>

    <update id="updateById">
        update t_permission_relation set
		permission_id = #{obj.permissionId},
		parent_permission_id = #{obj.parentPermissionId},
		data_state = #{obj.dataState},
		update_time = #{obj.updateTime}
		where id = #{id}
	</update>

    <update id="deleteByIdInLogic">
		update t_permission_relation 
		set data_state = 0,
		update_time = #{obj.updateTime}
		where id = #{id}
	</update>
</mapper>